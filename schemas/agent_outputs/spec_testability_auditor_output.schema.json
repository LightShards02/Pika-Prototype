{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SpecTestabilityAuditorOutput",
  "description": "Testability Auditor output. The agent scans SADS acceptance_criteria fields and emits manual_resolution_items for criteria that cannot be directly automated as tests. An empty array means all criteria are already testable. Each item includes a suggested_improvement rewriting the criterion in automatable form.",
  "type": "object",
  "required": ["manual_resolution_items"],
  "additionalProperties": false,
  "properties": {
    "manual_resolution_items": {
      "type": "array",
      "items": { "$ref": "#/$defs/testability_item" }
    }
  },
  "$defs": {
    "testability_item": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "item_id",
        "title",
        "spec_id",
        "field",
        "untestable_reason",
        "suggested_improvement",
        "suggested_test_type",
        "options"
      ],
      "properties": {
        "item_id": {
          "type": "string",
          "minLength": 1,
          "description": "Unique identifier for this resolution item (e.g. TEST-001)."
        },
        "title": {
          "type": "string",
          "minLength": 1,
          "description": "Short human-readable summary of the testability issue."
        },
        "spec_id": {
          "type": "string",
          "minLength": 1,
          "description": "Identifier of the spec row with untestable criteria."
        },
        "field": {
          "type": "string",
          "const": "acceptance_criteria",
          "description": "Always 'acceptance_criteria' — testability auditor only touches this field."
        },
        "untestable_reason": {
          "type": "string",
          "minLength": 1,
          "description": "Explanation of why the existing acceptance_criteria cannot be automated."
        },
        "suggested_improvement": {
          "type": "string",
          "minLength": 1,
          "description": "Agent-proposed rewrite of the acceptance_criteria in automatable form."
        },
        "suggested_test_type": {
          "type": "string",
          "enum": ["unit", "integration", "e2e", "manual"],
          "description": "Test type the suggested_improvement targets."
        },
        "options": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/resolution_option" },
          "description": "Resolution options presented to the user."
        }
      }
    },
    "resolution_option": {
      "type": "object",
      "additionalProperties": false,
      "required": ["option_id", "label", "effect"],
      "properties": {
        "option_id": {
          "type": "string",
          "enum": ["accept_suggestion", "let_agent_edit", "skip"],
          "description": "accept_suggestion: apply suggested_improvement; let_agent_edit: call spec_editor for a custom rewrite; skip: leave original unchanged."
        },
        "label": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable option label."
        },
        "effect": {
          "type": "string",
          "minLength": 1,
          "description": "Short description of what choosing this option does."
        }
      }
    }
  }
}
